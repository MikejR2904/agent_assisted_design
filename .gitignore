# We'll create .env.example/ and config/ examples once this project is concluded
.env
/config/

# Build artefacts and lock files
node_modules/
dist/
.next/
external/
package-lock.json
yarn.lock

# Hide run results
telemetry/
workspaces/

# RAG source documents (dropped in locally, not checked in)
/rag_sources/*
!/rag_sources/README.md

# Python SDK environments, caches, build artifacts, and durable local run ledgers
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.agent-runs/
.agent-runtime/
.agent-orchestrations/
.agent-telemetry/
.agent-audit-logs/
.agent-tool-results/
.agent-project-state/
