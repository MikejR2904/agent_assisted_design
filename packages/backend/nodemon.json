{
  "watch": ["src", "../../skills", "../../config"],
  "ext": "ts,json,toml,md",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "ts-node --project tsconfig.json -r tsconfig-paths/register src/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}